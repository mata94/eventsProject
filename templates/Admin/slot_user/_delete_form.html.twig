<form method="post" action="{{ path('app_slot_user_delete', {'id': slotUser.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ slotUser.id) }}">
    <button class="btn btn-sm btn-info">{{ 'general_delete'|trans }}</button>
</form>
