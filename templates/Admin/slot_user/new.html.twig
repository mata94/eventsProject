{% extends 'Admin/common/app.html.twig' %}

{% block title %}{{ 'title_createSlotUser'|trans }}{% endblock %}

{% block content %}
    <div class="row" style="height: 20px"></div>

    <div class="row">

        <div class="col-lg-6">

            <div class="panel panel-default">
                <div class="panel-heading">{{ 'title_createSlotUser'|trans }}</div>
                <div class="panel-body">
                    {% if error %}
                        <div class="alert alert-danger">{{ error }}</div>
                    {% endif %}
                    {{ include('Admin/slot_user/_form.html.twig') }}

                </div>
            </div>
        </div>

    </div>

{% endblock %}