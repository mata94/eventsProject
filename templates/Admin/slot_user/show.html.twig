<!DOCTYPE html>

<title>SlotUser</title>

{% block body %}
    <h1>SlotUser</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ slot_user.id }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ slot_user.createdAt ? slot_user.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_slot_user_index') }}">back to list</a>

    <a href="{{ path('app_slot_user_edit', {'id': slot_user.id}) }}">edit</a>

    {{ include('slot_user/_delete_form.html.twig') }}
{% endblock %}
