<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Information</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
        }

        .container {
            background-color: white;
            max-width: 600px;
            margin: 50px auto;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        h2 {
            text-align: center;
            color: #333;
        }

        .user-info {
            margin-top: 20px;
        }

        .user-info p {
            font-size: 16px;
            line-height: 1.6;
            margin: 10px 0;
            color: #555;
        }

        .user-info span {
            font-weight: bold;
            color: #000;
        }

        .button {
            display: block;
            text-align: center;
            margin-top: 20px;
        }

        .button a {
            background-color: #007bff;
            color: white;
            text-decoration: none;
            padding: 10px 20px;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }

        .button a:hover {
            background-color: #0056b3;
        }
        .alert-custom {
            background-color: #f8d7da;
            color: #721c24;
            border-color: #f5c6cb;
            padding: 10px;
            border-radius: 4px;
            margin-bottom: 10px;
            width: 60%;
        }
    </style>
</head>
<body>

<div class="container">
    <h2>User Details</h2>

    <div class="user-info">
        {% if slotUser.scanTime %}
            <div class="alert-custom">
                User has already scanned({{ slotUser.scanTime|date("d.m.Y H:i") }}).
            </div>
        {% endif %}
        <p><span>Full name:</span> {{ slotUser.user.fullName }}</p>
        <p><span>Email:</span> {{ slotUser.user.email }}</p>
        <p><span>Event Name:</span> {{ slotUser.slot.event.name }}</p>
        <p><span>Event Date:</span> {{ slotUser.slot.timeStart|date("d.m.Y") }}</p>
        <p><span>Time:</span> {{ slotUser.slot.timeEnd|date("H:i") }}</p>
    </div>
</div>

</body>
</html>
